name: release-package

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-*"

permissions:
  id-token: write # required for OIDC
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: softvisio/actions/release@main
