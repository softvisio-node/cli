<%_ if ( changelog.previousRelease && changelog.currentRelease ) { -%>
### <%- changelog.currentRelease.versionString %> (<%- changelog.currentRelease.changelogDate %>)
<%_ } else if ( !changelog.previousRelease && changelog.currentRelease ) { -%>
### <%- changelog.currentRelease.versionString %> (<%- changelog.currentRelease.changelogDate %>)
<%_ } else if ( changelog.previousRelease && !changelog.currentRelease ) { -%>
### Changes since the release: <%- changelog.previousRelease.versionString %> (<%- changelog.previousRelease.changelogDate %>)
<%_ } else { -%>
### Changes since the initial commit
<%_ } -%>
<%_ if ( !changelog.hasChanges ) { -%>

No changes since the previous release.
<%_ } else { -%>
<%_ if ( changelog.hasBreakingChanges ) { -%>

**Migration notes:**

See the list of the breaking changes below for details.
<%_ } -%>
<%_ for ( const [ title, property ] of Object.entries( blocks ) ) { -%>
<%_ if ( changelog.changes[ property ].length ) { -%>

**<%- title %>:**
<%_ for ( const change of changelog.changes[ property ] ) { -%>

- <%- change.getChangelogSubject() %>
<%_ if ( change.bodyText ) { -%>

<%- change.bodyText.replaceAll( /^(?!$)/gm, " ".repeat( 4 ) ) %>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ if ( compareUrl ) { -%>

Compare with the previous release: [`<%- changelog.previousRelease.versionString %>...<%- changelog.currentRelease.versionString %>`](<%- compareUrl %>)
<%_ } -%>
