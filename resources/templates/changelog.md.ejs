### <%- newVersion ? newVersion + " " : ""  -%>(<%- new Date().toISOString().substr( 0, 10 ) %>)
<%_ if ( !changes.hasChanges ) { -%>

No changes since the previous release.
<%_ } else { -%>
<%_ if ( changes.breakingChanges.length ) { -%>

**Breaking changes:**
<%_ for ( const change of changes.breakingChanges ) { -%>

- <%- change.subject %>
<%_ if ( change.bodyText ) { -%>

<%- change.bodyText.replaceAll( /^(?!$)/gm, "    " ) %>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ if ( changes.featureNonBreakingChanges.length ) { -%>

**Features:**
<%_ for ( const change of changes.featureNonBreakingChanges ) { -%>

- <%- change.subject %>
<%_ if ( change.bodyText ) { -%>

<%- change.bodyText.replaceAll( /^(?!$)/gm, "    " ) %>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ if ( changes.fixNonBreakingChanges.length ) { -%>

**Fixes:**
<%_ for ( const change of changes.fixNonBreakingChanges ) { -%>

- <%- change.subject %>
<%_ if ( change.bodyText ) { -%>

<%- change.bodyText.replaceAll( /^(?!$)/gm, "    " ) %>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ if ( changes.otherNonBreakingChanges.length ) { -%>

**Other changes:**
<%_ for ( const change of changes.otherNonBreakingChanges ) { -%>

- <%- change.subject %>
<%_ if ( change.bodyText ) { -%>

<%- change.bodyText.replaceAll( /^(?!$)/gm, "    " ) %>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ if ( compareUrl ) { -%>

Compare with the previous release: [`<%- previousVersion.toVersionString() %>...<%- newVersion.toVersionString() %>`](<%- compareUrl %>)
<%_ } -%>
<%_ if ( newVersion?.isMajor ) { -%>

Migration notes:

See the list of the breaking changes for details.
<%_ } -%>
