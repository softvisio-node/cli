### <%- newVersion %> (<%- new Date().toISOString().substr( 0, 10 ) %>)
<%_ if ( !changes.hasChanges ) { -%>

No changes since the previous release.
<%_ } else { -%>
<%_ if ( changes.breaking.length ) { -%>

Breaking changes:

<%_ for ( const change of changes.breaking ) { -%>
-   <%- change.subject %>
<%_ } -%>
<%_ } -%>
<%_ if ( changes.featNonBreaking.length ) { -%>

Features:

<%_ for ( const change of changes.featNonBreaking ) { -%>
-   <%- change.subject %>
<%_ } -%>
<%_ } -%>
<%_ if ( changes.fixNonBreaking.length ) { -%>

Fixes:

<%_ for ( const change of changes.fixNonBreaking ) { -%>
-   <%- change.subject %>
<%_ } -%>
<%_ } -%>
<%_ if ( changes.otherNonBreaking.length ) { -%>

Other:

<%_ for ( const change of changes.otherNonBreaking ) { -%>
-   <%- change.subject %>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ if ( compareUrl ) { -%>

[Compare with the previous release: <%- previousVersion.toVersionString() %> ... <%- newVersion.toVersionString() %>](<%- compareUrl %>)
<%_ } -%>
<%_ if ( newVersion.isMajor ) { -%>

Migration notes:

See the list of the breaking changes for details.
<%_ } -%>
