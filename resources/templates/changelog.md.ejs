<%_ if ( currentRelease ) { -%>
### <%- currentRelease.versionString %> (<%- currentRelease.changelogDate %>)
<%_ } else if ( changelog.previousRelease ) { -%>
### Changes since the release: <%- changelog.previousRelease.versionString %> (<%- changelog.previousRelease.changelogDate %>)
<%_ } else { -%>
### Changes since the initial commit
<%_ } -%>
<%_ if ( !changelog.hasChanges ) { -%>

No changes since the previous release.
<%_ } else { -%>
<%_ if ( changelog.hasBreakingChanges ) { -%>

**Migration notes:**

See the list of the breaking changes below for details.
<%_ } -%>
<%_ for ( const [ title, group ] of Object.entries( changesGroups ) ) { -%>
<%_ if ( group.changes.length ) { -%>

**<%- title %>:**
<%_ for ( const change of group.changes ) { -%>

- <%- change.getChangelogSubject() %>
<%_ if ( group.showAnnotatiion && change.bodyText ) { -%>

<%- change.bodyText.replaceAll( /^(?!$)/gm, " ".repeat( 4 ) ) %>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ } -%>
<%_ if ( compareUrl ) { -%>

Compare with the previous release: [<%- changelog.previousRelease.versionString %>](<%- compareUrl %>)
<%_ } -%>
