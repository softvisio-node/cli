### <%- newVersion %> (<%- new Date().toISOString().substr( 0, 10 ) %>)
<%_ if ( !changes.breaking.length && !changes.feat.length && !changes.fix.length ) { -%>

No significant changes since the previous release
<%_ } else { -%>
<%_ if ( changes.breaking.length ) { -%>

Breaking changes:

<%_ for ( const change of changes.breaking ) { -%>
-   <%- change.type %><%- change.scope ? `(${change.scope})` : "" %>: <%- change.description %>
<%_ } -%>
<%_ } -%>
<%_ if ( changes.feat.length ) { -%>

Features:

<%_ for ( const change of changes.feat ) { -%>
-   <%- change.scope ? `(${change.scope}): ` : "" %><%- change.description %>
<%_ } -%>
<%_ } -%>
<%_ if ( changes.fix.length ) { -%>

Fixes:

<%_ for ( const change of changes.fix ) { -%>
-   <%- change.scope ? `(${change.scope}): ` : "" %><%- change.description %>
<%_ } -%>
<%_ } -%>
<%_ } -%>
